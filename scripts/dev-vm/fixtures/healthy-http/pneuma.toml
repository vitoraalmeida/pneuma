schema_version = 3

[system]
name = "fixtures-test"

[application]
name = "healthy-http"

[delivery]
type = "oci"
image = "localhost:5000/healthy-http"

[runtime]
container_port = 8080
healthcheck_path = "/healthz"
expected_status = 200

[exposure]
default_visibility = "internal"
