schema_version = 3

[system]
name = "fixtures-test"

[application]
name = "redirect-public"

[delivery]
type = "oci"
image = "localhost:5000/redirect-public"

[runtime]
container_port = 8080
healthcheck_path = "/healthz"
expected_status = 200

[exposure]
default_visibility = "public"
domain = "redirect-public.pneuma.test"
