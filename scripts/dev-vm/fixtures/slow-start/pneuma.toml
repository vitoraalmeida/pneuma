schema_version = 2

[system]
name = "fixtures-test"

[application]
name = "slow-start"

[delivery]
type = "oci"
image = "localhost:5000/slow-start"

[source]
repository = "local"
branch = "main"

[runtime]
container_port = 8080
healthcheck_path = "/healthz"
expected_status = 200

[exposure]
default_visibility = "internal"
