schema_version = 3

[system]
name = "oci-only-system"

[application]
name = "oci-only-app"

[delivery]
type = "oci"
image = "registry.example/team/service"

[runtime]
container_port = 8080
healthcheck_path = "/healthz"
expected_status = 200

[exposure]
default_visibility = "internal"
